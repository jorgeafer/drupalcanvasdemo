(function (Drupal) {
  'use strict';

  // Placeholder for theme-level Drupal behaviors.
  // Component-level JS lives alongside each component's .twig and .css.
  Drupal.behaviors.tibidabo = {
    attach: function (context, settings) {}
  };
}(Drupal));
